# ============================================================
# Realtime-Transcription 依赖
# ============================================================
# Python: 3.11+（测试环境 3.11.7）
#
# ⚠️ 版本兼容性说明：
#   - funasr 1.3.9 需要 torch>=2.1，但未在 requirements 中固定 torch 版本
#     建议安装匹配 CUDA 版本的 PyTorch：https://pytorch.org/get-started/locally/
#     已知兼容组合：torch 2.5.1 + numpy 2.3.5 + CUDA 12.4
#   - numpy 2.x 与 torch<2.2 不兼容，若 torch 低于 2.2 需降级到 numpy<2
#   - PyAudioWPatch 依赖 PortAudio，Windows 下已包含预编译二进制

funasr==1.3.9
websockets==16.0
fastapi==0.136.3
uvicorn==0.48.0
numpy==2.3.5
PyAudioWPatch==0.2.12.8
# torch 需根据 CUDA 版本单独安装，此处不固定：
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
